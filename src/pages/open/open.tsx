import { Box } from '@mantine/core';
import { Table } from '../../components';

const Open = () => {

  return (
    <Box>
      <Table currentStatus='OPEN' />
    </Box>
  );
};

export default Open;
